<%= simple_form_for([@post, @post.replies.build], remote: true) do |f| %>
<div class="field">
</div>

<div class="control">
  <%= f.input :reply, input_html: {class: 'textarea'}, wrapper: false, label_html: {class: 'label'} %>
</div>

<% end %>
